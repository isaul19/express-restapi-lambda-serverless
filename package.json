{
  "name": "backend-express-lambda-serverless",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "serverless offline start",
    "dev": "serverless offline start --reloadHandler",
    "deploy": "rm -rf node_modules && npm install --omit=dev && serverless deploy && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "serverless-dotenv-plugin": "^6.0.0",
    "serverless-offline": "^13.3.3"
  }
}
